import PageHeader from "@/components/PageHeader";
import { ManageAccounts } from "@mui/icons-material";

export default function UserAssignmentPageHeader() {
  return <PageHeader pageTitle="User Assignment" pageIcon={ManageAccounts} />;
}
